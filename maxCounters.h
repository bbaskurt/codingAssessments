#pragma once
#include <vector>

// codility test score: %66 (correctness:%100 but high complexity)
class maxCounters
{
public:
	maxCounters();
	~maxCounters();

	std::vector<int> solution(int N, std::vector<int> &A);
};

